apiVersion: v1
kind: Service
metadata:
  name: frontend-lab901 
  labels:
    name: frontend-lab901 
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: frontend-lab901 
  type: NodePort
